node_modules/
.env
.vercel

# Local-only files never intended for the repo (not project source)
*.docx
~$*
*.log

# Local case-run history (server.js) - runtime data, not source
data/case-history.json
